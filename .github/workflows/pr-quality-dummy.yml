name: PR Quality Checks (Dummy)

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

jobs:
  quality-dummy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read

    steps:
      - name: Start dummy quality checks
        run: |
          echo "[quality] Starting dummy quality checks"
          echo "[quality] PR #${{ github.event.pull_request.number }}"
          echo "[quality] files changed: (placeholder)"
          echo "[quality] (add ESLint/tsc/Prettier/CodeQL etc. later)"
